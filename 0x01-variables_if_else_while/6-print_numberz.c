#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/** 
*main - Classifies neg, pos & zero 
*Return: Always (success)
*/
int main(void) 

{

int w;

for (w = '0'; w <= '9'; w++)
{
putchar(w);
}
putchar('\n');
return (0);
}
