#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/** 
*main - Classifies neg, pos & zero 
*Return: Always (success)
*/
int main(void)
{

char z;

for (z = 'a', z <= 'z', z++)
{
while (z != e, z != q)
{
putchar(z);
}

putchar('\n');
return (0);
}

