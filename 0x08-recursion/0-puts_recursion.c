#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 * _puts_recursion - a function that prints a string and returns a new line
 * @s: string to be printed
 * Return: string
 */
void _puts_recursion(char *s)
{
	puts(s);
}
