#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen_recursion - prints length of a string
 * @s: string to be printed
 * Return: Success
 */
int _strlen_recursion(char *s)
{
	int n;

	n = strlen(s);

	return (n);
}
