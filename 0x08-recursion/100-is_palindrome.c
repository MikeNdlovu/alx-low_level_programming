#include "main.h"
#include <stdio.h>
#include <string.h>
#include <stdlib.h>

/**
 * is_palindrome - returns 1 if s is a palindrome
 * @s: string to ber checked
 * Return: Success
 */
int is_palindrome(char *s)
{
	int n = 0;

	if (s[1] != '\0')
	{
		return (1);
	}

	return (0);
}
