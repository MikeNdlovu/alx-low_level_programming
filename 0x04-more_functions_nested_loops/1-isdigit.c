#include "main.h"
#include <stdio.h>

/**
 * _isdigit - checks numbers
 * @c: digit to be checked
 * Return: 0 if otherwise 1 if digit
 */
int _isdigit(int c)
{
	if (c <= 9)
	{
	return (1);
	}
	return (0);
}
