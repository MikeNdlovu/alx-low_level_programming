#include "main.h"

/**
 * swap_int - swaps 2 int values
 * @a: swaps to b
 * @b: swaps to a
 * Return: 0 always
 */

void swap_int(int *a, int *b)
{
	if (*a <= *b && a* >= *b)
	{
	_putchar(*b +'0');
	}
	else 
	{
	_putchar(*b + '0');
}
