#include "main.h"

/**
 * swap_int - swaps 2 int values
 * @a: swaps to b
 * @b: swaps to a
 * Return: 0 always
 */

int modif(int *c)
{
	int z = 0;
	
	if (*a < z)
	{
	*a = *b;
	}
	else if (*a > z)
	{
	*a = *b;
	}
	else if (*b > z)
	{
	*b = *a;
	}
	else if (*b < z)
	{
	*b = *a;
	}
}
