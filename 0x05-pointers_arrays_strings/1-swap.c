#include "main.h"

/**
 * swap_int - swaps 2 int values
 * @a: swaps to b
 * @b: swaps to a
 * Return: 0 always
 */

int modif(int *c)
{
        *c = *a;
}

void swap_int(int *a, int *b)
{
*a = *b;
modif(*b);           
}
