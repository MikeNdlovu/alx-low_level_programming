#include "main.h"

/**
 * swap_int - swaps 2 int values
 * @a: swaps to b
 * @b: swaps to a
 * Return: 0 always
 */

void swap_int(int *a, int *b);
{
	int z = 0;
	
	if (*a < z)
	{
	*a = *b;
	}
	else if (*a > z)
	{
	*a = *b;
	}
	else if (*b > z)
	{
	*b = *a;
	}
	else if (*b < z)
	{
	*b = *a;
	}
}
